# 公开源码验证

日期：2026-09-18。版本：0.7.0（15）。本次使用 Xcode 27 工具链，在 macOS 上验证。

| 检查 | 结果 |
| --- | --- |
| `swift test` | 54 项通过；新增自然月、自然季度、跨年、时区、夏令时、待办过滤、子任务上下文及手机返回路径覆盖 |
| Mac Preview + CLI | 构建通过；实际打开四个时间视图，核对日期范围，并验证新增今日任务后直接打开详情 |
| iOS Preview | iOS 模拟器及分享扩展构建通过；模拟器启动和首页截图确认方向在先、时间入口在后 |
| 个人兼容构建 | Mac 与 iPhone 签名构建通过并安装 0.7.0（15）；保留既有 App 标识、容器、数据目录及环境；Mac 本地 Agent 的四个时间查询成功，iCloud 状态正常 |
| 公开内容 | 新增代码、测试和文档；不含个人任务、运行库、私人验收截图或签名文件 |

本次没有完成 iPhone 真机的逐项点击验收；手机返回路径已有核心回归测试。没有以新的跨设备写入测试验证同步。默认首页未切换为今天，任务归属和同步存储机制没有改动。

Preview 不连接 iCloud。Fork 后的签名、真实设备安装、两端同步和正式分发需要使用自己的配置重新验证。iPad 保留布局兼容，没有本次 iPad 真机验收。iOS 富文本委托仍有系统 API 弃用警告，当前编译通过。

GitHub Actions 检查核心测试及 Mac Preview；云端 CI 状态以仓库实际运行结果为准，不把本地通过视为 CI 已通过。
